
  # Spike Tournament System

  This is a code bundle for Spike Tournament System. The original project is available at https://www.figma.com/design/u5oclcyuRivYW3C5Mzdf6a/Spike-Tournament-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  